
create_cylinder :: (half_height: float, radius: float, subdivision: int, with_normals: bool = false, with_texcoords: bool = false, should_upload: bool = true) -> *Geometry {
    assert(false);
    return null;
}
